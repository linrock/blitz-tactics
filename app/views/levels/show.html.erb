<% content_for(:title) { @level.name } %>

<%= render :partial => "interface/main",
           :locals  => {
             :level_id    => @level.number,
             :level_name  => @level.name,
             :round_times => @round_times
           } %>

<script>
  $(function() {
    blitz.levelId = <%= @level.id %>;
  });
</script>
